<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ng.softcom.bespoke.craftadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.CAMainActivity"
            android:label="@string/title_activity_camain"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CALoginActivity"
            android:label="@string/login_activity_title"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="CLOUDINARY_URL"
            android:value="cloudinary://677199261285745:R9vyA4_nis3V7Fi--dk0GOfRDFw@softcom" />

        <activity android:name=".activities.CAArtisanDetail" />
        <activity android:name=".activities.CANewArtisan" />
        <activity android:name=".activities.CAEditActivity"/>
    </application>

</manifest>
